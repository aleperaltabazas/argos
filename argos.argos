command(compile) {
  option(long(help), short(h)),
  option(long(source), short(s)),
  option(long(target), short(t))
}

command(complete) {
  option(long(help), short(h)),
  option(long(options), short(o))
}
